# No-Drift Rules

These rules apply to AISRAF SAS Prototype v0.1.2 unless a later Build Package contract explicitly authorizes a narrower change.

- No unlisted files or folders.
- No release artifacts before Build Package 15.
- No diagrams before Build Package 13.
- No run outputs before Build Package 11.
- No prompt content before Build Package 04 except `prompts/README.md`. Build Package 04 authorizes the registry, two family READMEs, and the 23 `*.prompt.md` cards under `prompts/rs/` and `prompts/dfd/` only.
- Build Package 04 prompt cards must use only run-profile variables (`{{run_id}}`, `{{input_root}}`, `{{expected_root}}`, `{{output_root}}`, `{{mode}}`, `{{postback_execution_status}}`, `{{output_destination_mode}}`). No prompt may hardcode `run_id`, `sample_id`, paths, source/destination URLs, `output_destination_mode`, or `postback_execution_status`.
- Build Package 04 prompt cards must write only under `{{output_root}}`. No prompt may write to `config/`, `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, `blueprints/`, `templates/`, `samples/`, `diagrams/`, `docs/`, `release/`, `tools/`, or `validation/`. No prompt may write to the old reference workspace.
- Build Package 04 prompt cards must not invent run-profile fields outside `config/run-profile.schema.yaml`. Adding a field requires a Build Package 02 schema change in the same change set.
- Build Package 04 prompt cards must not require a Build Package 05+ artifact (skill contract, PRA specification, `.agent.md` adapter, catalog, blueprint, template, sample, run output, diagram, docs/runbook, release artifact) as a hard read. Future-package references must be marked explicitly future-only and the prompt must not stop on their absence.
- Build Package 04 prompt cards must not claim execution of skills, PRAs, `.agent.md` adapters, Jira post-back, Confluence publication, Rovo execution, MCP execution, runtime, cloud, database, Terraform, or ADK services. Cards must not claim diagram generation or release/export.
- Build Package 04 accuracy scoring is owned by `prompts/rs/13-accuracy-score.prompt.md` only and is gated on `scoring_enabled: true`, `expected_baseline_required: true`, and a non-empty populated `{{expected_root}}`. No other prompt may produce a `qualitative_score` field. Scoring outputs may not edit baselines under `{{expected_root}}`.
- Build Package 04 prompt count is fixed at 23 (14 RS + 9 DFD). The full-chain wrapper `prompts/rs/00-run-full-chain.prompt.md` is `status: planned`, writes no output of its own, and may not claim orchestration or full-chain automation. RS output `12-targeted-questions.md` is produced by `prompts/rs/07-missing-fact-question-generate.prompt.md` together with `09-missing-facts.md`; no separate targeted-question prompt may be created.
- Build Package 04 must not create skill contracts, `skills/skill-registry.yaml`, PRA specifications, `prototype-agents/prototype-agent-registry.yaml`, `.agent.md` adapters, catalogs, blueprints, templates beyond authoring-agent templates, samples, expected baselines, run outputs, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, or release-export tools.
- Build Package 04 may extend `tools/Test-AisrafPackage.ps1` only for compatibility (split Check 08 to allow `prompts/` content matching the registry; extend Check 11's allowed validation file list). Build Package 04 must not modify `validation/package-03-tools-checklist.md`, which is frozen Package 03 acceptance evidence.
- No skill content before Build Package 05 except `skills/README.md`. Build Package 05 authorizes `skills/skill-registry.yaml`, `skills/rs/README.md`, `skills/dfd/README.md`, the 17 RS skill contracts under `skills/rs/SK-*.md`, and the 9 DFD subskill contracts under `skills/dfd/SK-DFD-0[1-9]-*.md`.
- Build Package 05 skill contracts must use only run-profile variables (`{{run_id}}`, `{{input_root}}`, `{{expected_root}}`, `{{output_root}}`, `{{mode}}`, `{{postback_execution_status}}`, `{{output_destination_mode}}`). No skill may hardcode `run_id`, `sample_id`, paths, source/destination URLs, `output_destination_mode`, or `postback_execution_status`.
- Build Package 05 skill contracts must declare write paths only under `{{output_root}}` (or `{{output_root}}/dfd/` for DFD subskills) when invoked through their mapped Build Package 04 prompt card. No skill may declare a write to `config/`, `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, `blueprints/`, `templates/`, `samples/`, `diagrams/`, `docs/`, `release/`, `tools/`, or `validation/`. No skill may declare a write to the old reference workspace.
- Build Package 05 skill contracts must not invent run-profile fields outside `config/run-profile.schema.yaml`. Adding a field requires a Build Package 02 schema change in the same change set.
- Build Package 05 skill contracts must not require a Build Package 06+ artifact (PRA specification, `.agent.md` adapter, catalog, blueprint, template, sample, run output, diagram, docs/runbook, release artifact) as a hard read. Future-package references must be marked explicitly future-only and the skill must not stop on their absence.
- Build Package 05 skill contracts must not claim execution of PRAs, `.agent.md` adapters, Jira post-back, Confluence publication, Rovo execution, MCP execution, runtime, cloud, database, Terraform, or ADK services. Skills must not claim diagram generation or release/export. Only `skills/rs/SK-ACCURACY-SCORE.md` produces an accuracy score and only when the mapped prompt's scoring conditions are met; no other skill may produce a numeric accuracy score.
- Build Package 05 skill contracts must not modify any file under `prompts/` or any file under `{{expected_root}}`. The Package 04 prompt registry is sealed for the duration of Build Package 05; the canonical Package 05 skill names diverge from two Package 04 prompt-registry placeholder names (`SK-DFD-LEGEND-NORMALIZE` → `SK-LEGEND-NORMALIZE`, `SK-SECURITY-STACK-DETECT` → `SK-SECURITY-STACK-ASSESS`). The drift is documented in `skills/skill-registry.yaml` `compatibility_notes`; no edit to the prompt registry is permitted.
- Build Package 05 skill count is fixed at 26 (17 RS + 9 DFD). The full-chain wrapper placeholder `SK-CHAIN-WRAPPER` referenced by `prompts/rs/00-run-full-chain.prompt.md` is recorded only under `planned_future_skills` in the skill registry and is not counted in the 26.
- Build Package 05 must not create skill-execution code, PRA specifications, `prototype-agents/prototype-agent-registry.yaml`, `.agent.md` adapters, catalogs, blueprints, templates beyond authoring-agent templates, samples, expected baselines, run outputs, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, or release-export tools.
- Build Package 05 may extend `tools/Test-AisrafPackage.ps1` only for the four authorized compatibility changes: (a) update header/synopsis text; (b) remove `skills/` from the README-only folder rule; (c) add Check 08c-skills-content-limits enforcing the approved skill-folder layout; (d) extend Check 11's allowed validation-file list to include `package-05-skills-checklist.md` and `skill-registry-checklist.md`. Build Package 05 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, or `validation/prompt-registry-checklist.md`.
- No PRA or `.agent.md` adapter content before Build Package 06 except the Build Package 01 README placeholders under `prototype-agents/` and `.agents/`. Build Package 06 authorizes `prototype-agents/prototype-agent-registry.yaml`, `prototype-agents/prototype-agent-template.md`, exactly 8 `prototype-agents/PRA-0[1-8]-*.md` files, the `prototype-agents/README.md` overwrite, exactly 7 `.agents/aisraf-*.agent.md` files (orchestrator, input-reader, dfd-extractor, review-table-builder, blueprint-questioner, finding-recommender, handoff-qa-scorer), and the `.agents/README.md` overwrite.
- Build Package 06 PRA specs and `.agent.md` adapters must use only run-profile variables (`{{run_id}}`, `{{input_root}}`, `{{expected_root}}`, `{{output_root}}`, `{{mode}}`, `{{postback_execution_status}}`, `{{output_destination_mode}}`). No PRA or adapter may introduce non-schema variables such as `{{catalog_root}}`, `{{prompt_root}}`, `{{skill_root}}`, `{{template_root}}`, `{{diagram_root}}`, or `{{validation_root}}`. Canonical artefact paths are referenced as repository-relative literals (for example `prompts/rs/01-input-package-read.prompt.md`, `skills/rs/SK-INPUT-PACKAGE-READ.md`).
- Build Package 06 PRA specs and `.agent.md` adapters must declare write paths only under `{{output_root}}` (or `{{output_root}}/dfd/` for PRA-03's DFD subskill outputs and the AISRAF DFD Extractor adapter). No PRA or adapter may declare a write to `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `config/`, `tools/`, `validation/`, `catalogs/`, `blueprints/`, `templates/`, `samples/`, `diagrams/`, `docs/`, `release/`, `authoring-agents/`, `.github/`, root files, or any path under `{{expected_root}}`. No PRA or adapter may declare a write to the old reference workspace.
- Build Package 06 PRA specs and `.agent.md` adapters must not invent run-profile fields outside `config/run-profile.schema.yaml`. Adding a field requires a Build Package 02 schema change in the same change set.
- Build Package 06 PRA specs and `.agent.md` adapters must not modify any file under `prompts/`, `skills/`, `prompts/prompt-registry.yaml`, `skills/skill-registry.yaml`, or any baseline under `{{expected_root}}`. The Package 04 prompt registry and the Package 05 skill registry are sealed for the duration of Build Package 06; concrete `future_pra_owner` resolution is recorded in `prototype-agents/prototype-agent-registry.yaml#prompt_skill_agent_crosswalk` and `validation/prompt-skill-agent-mapping-checklist.md` (founder decision Q2). No edit to either upstream registry is permitted.
- Build Package 06 PRA specs and `.agent.md` adapters must not claim execution of runtime, cloud, Google ADK, Vertex/GCP, MCP, Jira post-back, Confluence publication, Rovo execution, database, Terraform, release packaging, diagram generation, or accuracy scoring outside `skills/rs/SK-ACCURACY-SCORE.md`. Selecting a `.agent.md` adapter from the local Copilot Chat dropdown is not equivalent to runtime execution.
- Build Package 06 `.agent.md` adapters must not duplicate the full body of any prompt card or skill contract; adapters reference canonical paths only. Adapter YAML frontmatter must contain `name`, `description`, `tools: [read, search, edit]`, `user-invocable: true`, and `handoffs`, and must not pin a Copilot Chat model. No adapter may declare a tool other than `read`, `search`, or `edit`.
- Build Package 06 PRA count is fixed at 8 (`PRA-01-SAS-REVIEW-ORCHESTRATOR` through `PRA-08-HANDOFF-QA-SCORER`). Build Package 06 adapter count is fixed at 7 (orchestrator, input-reader, dfd-extractor, review-table-builder, blueprint-questioner, finding-recommender, handoff-qa-scorer). `PRA-04-LEGEND-NORMALIZER` has no dedicated adapter and is wrapped jointly with `PRA-03-DFD-EXTRACTOR` by `.agents/aisraf-dfd-extractor.agent.md` (founder decision Q1). No `aisraf-legend-normalizer.agent.md` may be created.
- Build Package 06 must map all 23 Build Package 04 prompt cards and all 26 Build Package 05 skill contracts into the prompt-skill-agent crosswalk exactly once each. `prompts/rs/00-run-full-chain.prompt.md` (PR-RS-00) is mapped to `PRA-01-SAS-REVIEW-ORCHESTRATOR` with no skill (consistent with `skills/skill-registry.yaml#planned_future_skills` for `SK-CHAIN-WRAPPER`).
- Build Package 06 must record `planned_future_adk_alignment` as `status: future_only` with `runtime_deployed: false`, `adk_deployed: false`, `cloud_resources_created: false`, `database_jobs_created: false`, `mcp_execution_available: false`, and `jira_confluence_execution_available: false`.
- Build Package 06 must not create catalogs, blueprints, templates beyond authoring-agent templates, samples, expected baselines, run outputs, diagrams, docs/runbooks (including `docs/mappings/*` — the old v0.01 mapping documents are collapsed into `prototype-agents/prototype-agent-registry.yaml#prompt_skill_agent_crosswalk` and `validation/prompt-skill-agent-mapping-checklist.md` per founder decision Q3), release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, runtime services, cloud assets, deployed agents, MCP/Jira/Confluence/Rovo execution proof, ADK or Vertex/GCP code, database jobs, Terraform, release-export tools, or release binaries.
- Build Package 06 may extend `tools/Test-AisrafPackage.ps1` only for the six authorized compatibility changes: (a) update header/synopsis text from "Build Package 01–05" to "Build Package 01–06"; (b) replace Check 07 with a Build Package 06 adapter rule that enforces exactly 7 approved `.agent.md` files under `.agents/`; (c) add a new check enforcing the `prototype-agents/` content surface (README.md, prototype-agent-registry.yaml, prototype-agent-template.md, and 8 `PRA-0[1-8]-*.md` files only); (d) remove `prototype-agents/` and `.agents/` from the README-only folder rule; (e) extend Check 11's allowed validation-file list with `package-06-agents-checklist.md`, `agent-registry-checklist.md`, and `prompt-skill-agent-mapping-checklist.md`; (f) preserve every other existing validator behaviour. Build Package 06 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, `validation/prompt-registry-checklist.md`, `validation/package-05-skills-checklist.md`, or `validation/skill-registry-checklist.md`, which are frozen acceptance evidence.
- No catalog content before Build Package 07 except `catalogs/README.md`. Build Package 07 authorizes the `catalogs/README.md` overwrite, `catalogs/catalog-registry.yaml`, the seven family folders (`components/`, `interactions/`, `boundaries/`, `identity-access/`, `data-protection/`, `security-stacks/`, `review/`) each with a `README.md`, and exactly the 24 controlled-vocabulary YAML catalogs listed in `PACKAGE-MANIFEST.yaml#build_package_07_allowed_writes.catalog_files`.
- Build Package 07 catalogs must use only YAML for controlled vocabulary; the only Markdown files allowed under `catalogs/` are the 8 READMEs (root + 7 family). Every catalog YAML must include the required top-level fields (`catalog_id`, `catalog_name`, `owning_build_package: Build Package 07`, `status`, `purpose`, `consumers`, `entries`, `unknown_value`, `evidence_rules`, `prohibited_claims`, `version_notes`, `source_reference`, `controlled_value_policy`, `anti_inference_rules`) and every entry must include the required entry fields (`id`, `label`, `description`, `when_to_use`, `when_not_to_use`, `evidence_required`, `confidence_guidance`, `notes`).
- Build Package 07 must mark `catalogs/security-stacks/proof-vs-signal-rule-catalog.yaml` with `global_rule: true` and `applies_to_all_catalogs: true`, and every other catalog must reference it in `evidence_rules`. Build Package 07 must mark `catalogs/data-protection/confidence-level-catalog.yaml` with `cross_cutting_catalog: true`, `primary_family: data-protection`, and an `applies_to:` list covering DFD extraction, annotation resolution, component classification, boundary classification, finding confidence, and accuracy scoring.
- Build Package 07 catalogs must not record blueprint identifiers (`BP-*`); future blueprint consumption is recorded only as `status: future_only` inside `catalogs/catalog-registry.yaml#future_blueprint_consumption_notes`.
- Build Package 07 catalogs must not introduce non-schema variables (`{{catalog_root}}`, `{{prompt_root}}`, `{{skill_root}}`, `{{template_root}}`, `{{diagram_root}}`, `{{validation_root}}`); repository-relative literals are used everywhere. The existing reference to `{{catalog_root}}` inside the Build Package 06 `prototype-agents/prototype-agent-registry.yaml#rejected_non_schema_variables` list (the registry's "rejected variables" listing) is documented as `known_upstream_drift: true` inside `catalogs/catalog-registry.yaml#compatibility_notes` and `validation/catalog-consumption-checklist.md`; Build Package 07 must not edit the prototype-agent registry.
- Build Package 07 must not contain recommendation prose, handoff text, template language, or scripted remediation language inside any catalog. Recommendation prose belongs to Build Package 09 templates; runbook prose belongs to Build Package 14. The recommendation-type catalog records categories only.
- Build Package 07 must not compute AI Action Level, severity, or scoring outcomes inside any catalog. Computation/selection is owned by `skills/rs/SK-AI-ACTION-LEVEL-CLASSIFY.md`, `skills/rs/SK-FINDING-CLASSIFY.md`, `skills/rs/SK-ACCURACY-SCORE.md`, and human review.
- Build Package 07 must not modify any file under `prompts/`, `skills/`, `prototype-agents/`, or `.agents/`, and must not modify `prompts/prompt-registry.yaml`, `skills/skill-registry.yaml`, or `prototype-agents/prototype-agent-registry.yaml`. The Build Package 04 prompt registry, the Build Package 05 skill registry, and the Build Package 06 prototype-agent registry are sealed for the duration of Build Package 07; consumer mapping is recorded only in `catalogs/catalog-registry.yaml` and `validation/catalog-consumption-checklist.md`.
- Build Package 07 catalog count is fixed at 24 controlled-vocabulary YAML catalogs (3+3+3+3+4+3+5 across the seven families) plus 1 catalog registry plus 8 READMEs = 33 total files under `catalogs/`. The Build Package 07 founder approval (Q7) recorded `catalog_count: 28` and 38 total files alongside the explicit file list; the explicit file list takes precedence and the documented arithmetic discrepancy is captured in `catalogs/catalog-registry.yaml#compatibility_notes.documented_count_arithmetic_discrepancy`.
- Build Package 07 must not create blueprints, templates beyond authoring-agent templates, samples, expected baselines, run outputs, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, runtime services, cloud assets, deployed agents, MCP/Jira/Confluence/Rovo execution proof, ADK or Vertex/GCP code, database jobs, Terraform, release-export tools, or release binaries.
- Build Package 07 may extend `tools/Test-AisrafPackage.ps1` only for the five authorized compatibility changes: (a) update header/synopsis text from "Build Package 01-06" to "Build Package 01-07"; (b) remove `catalogs/` from the Check 08 README-only folder rule; (c) add Check `08e-catalogs-content-limits` enforcing the exact Package 07 catalog folder layout (README.md + catalog-registry.yaml + 7 family folders, each with README.md + listed YAML files only); (d) extend Check 11's allowed validation-file list with `package-07-catalogs-checklist.md`, `catalog-registry-checklist.md`, and `catalog-consumption-checklist.md`; (e) preserve every other existing validator behaviour. Build Package 07 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, `validation/prompt-registry-checklist.md`, `validation/package-05-skills-checklist.md`, `validation/skill-registry-checklist.md`, `validation/package-06-agents-checklist.md`, `validation/agent-registry-checklist.md`, or `validation/prompt-skill-agent-mapping-checklist.md`, which are frozen acceptance evidence.
- No blueprints before Build Package 08 except `blueprints/README.md`. Build Package 08 authorizes the `blueprints/README.md` overwrite, `blueprints/blueprint-registry.yaml`, `blueprints/blueprint-template.yaml`, the two category folders (`platform-independent/`, `cloud-patterns/`) each with a `README.md`, and exactly the 9 controlled YAML blueprints listed in `PACKAGE-MANIFEST.yaml#build_package_08_allowed_writes.blueprint_files`.
- Build Package 08 blueprints must use only YAML for blueprint content; the only Markdown files allowed under `blueprints/` are the 3 READMEs (root + 2 category). Every blueprint YAML must include the 19 required fields (`blueprint_id`, `blueprint_name`, `owning_build_package: Build Package 08`, `status`, `purpose`, `applicability`, `catalog_dependencies`, `required_inputs`, `match_conditions`, `candidate_conditions`, `no_match_conditions`, `unknown_conditions`, `material_missing_facts`, `supported_finding_categories`, `supported_recommendation_types`, `human_review_gate`, `prohibited_claims`, `source_reference`, `version_notes`) plus the three required policy blocks (`match_evidence_policy`, `catalog_value_policy`, `output_boundary`) plus the `runtime_and_external_execution` block carrying six false flags (founder decision Q8).
- Build Package 08 must use the four-state match model `[matched, candidate, no_match, unknown]` (founder decision Q1). Every BP-*.yaml must populate every match-state condition block with at least one entry. The old v0.01 three-state model is not permitted.
- Build Package 08 must not introduce new catalog identifiers or new controlled values inside any blueprint. Every `catalog_id` cited in `catalog_dependencies`, every `catalog_reference` cited in `material_missing_facts`, and every `catalog: catalogs/...` path cited in `supported_finding_categories` and `supported_recommendation_types` must resolve to an existing entry in `catalogs/catalog-registry.yaml#catalogs[]`. If a controlled value is required but missing, the blueprint records it as a `material_missing_fact` and a separate governed catalog update is required to add it.
- Build Package 08 must not contain final recommendation prose, handoff text, validation-ticket prose, owner routing, severity computation, scoring computation, or AI Action Level computation inside any blueprint. Recommendation prose belongs to Build Package 09 templates; runbook prose belongs to Build Package 14; severity selection is owned by `skills/rs/SK-FINDING-CLASSIFY.md`; AI Action Level selection is owned by `skills/rs/SK-AI-ACTION-LEVEL-CLASSIFY.md`; final disposition is owned by human review.
- Build Package 08 must not modify any file under `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, or `catalogs/`, and must not modify `prompts/prompt-registry.yaml`, `skills/skill-registry.yaml`, `prototype-agents/prototype-agent-registry.yaml`, or `catalogs/catalog-registry.yaml`. The Build Package 04 prompt registry, Build Package 05 skill registry, Build Package 06 prototype-agent registry, and Build Package 07 catalog registry are sealed for the duration of Build Package 08 (founder decision Q4); blueprint→catalog, blueprint→skill, blueprint→PRA, blueprint→adapter, and blueprint→prompt consumer maps are recorded only in `blueprints/blueprint-registry.yaml` and in `validation/blueprint-registry-checklist.md` / `validation/blueprint-catalog-consumption-checklist.md`. The Build Package 07 catalog count remains locked at 24 controlled-vocabulary YAML catalogs and 33 total files under `catalogs/`.
- Build Package 08 blueprint count is fixed at 9 controlled YAML blueprints (8 platform-independent + 1 cloud-pattern) plus 1 blueprint registry plus 1 blueprint template plus 3 READMEs = 14 total files under `blueprints/`. Founder decision Q2 disallows a 10th blueprint, fine-tuning, multimodal, vector-governance, model-evaluation, and CSP-specific variants in this package.
- Build Package 08 must not claim runtime, cloud, Google ADK, Vertex/GCP, MCP, Jira post-back, Confluence publication, Rovo execution, database, Terraform, release packaging, diagram generation, or accuracy scoring inside any blueprint. Every BP-*.yaml and `blueprints/blueprint-registry.yaml` must explicitly carry the six false flags (`runtime_deployed: false`, `adk_deployed: false`, `cloud_resources_created: false`, `database_jobs_created: false`, `mcp_execution_available: false`, `jira_confluence_execution_available: false`). A blueprint match is a review hypothesis, not an approval claim or implementation proof.
- Build Package 08 must not introduce non-schema variables (`{{catalog_root}}`, `{{prompt_root}}`, `{{skill_root}}`, `{{template_root}}`, `{{diagram_root}}`, `{{validation_root}}`); repository-relative literals are used everywhere. Variables `{{run_id}}`, `{{input_root}}`, `{{expected_root}}`, `{{output_root}}`, `{{mode}}`, `{{postback_execution_status}}`, and `{{output_destination_mode}}` must not appear inside any blueprint either, since blueprints do not write outputs.
- Build Package 08 must not create catalogs, prompt cards, skill contracts, PRA specs, `.agent.md` adapters, templates beyond authoring-agent templates, samples, expected baselines, run outputs, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, runtime services, cloud assets, deployed agents, MCP/Jira/Confluence/Rovo execution proof, ADK or Vertex/GCP code, database jobs, Terraform, release-export tools, or release binaries.
- Build Package 08 may extend `tools/Test-AisrafPackage.ps1` only for the five authorized compatibility changes (founder decision Q5): (a) update header/synopsis text from "Build Package 01-07" to "Build Package 01-08"; (b) remove `blueprints/` from the README-only folder rule; (c) add Check `08f-blueprints-content-limits` enforcing the exact Package 08 blueprint folder layout; (d) extend Check 11's allowed validation-file list with `package-08-blueprints-checklist.md`, `blueprint-registry-checklist.md`, and `blueprint-catalog-consumption-checklist.md`; (e) preserve every other existing validator behaviour. Build Package 08 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, `validation/prompt-registry-checklist.md`, `validation/package-05-skills-checklist.md`, `validation/skill-registry-checklist.md`, `validation/package-06-agents-checklist.md`, `validation/agent-registry-checklist.md`, `validation/prompt-skill-agent-mapping-checklist.md`, `validation/package-07-catalogs-checklist.md`, `validation/catalog-registry-checklist.md`, or `validation/catalog-consumption-checklist.md`, which are frozen acceptance evidence.
- No templates before Build Package 09 except Build Package 01 authoring-agent templates. Build Package 09 authorizes the `templates/README.md` overwrite, `templates/template-registry.yaml`, the four family folders (`output/`, `jira/`, `confluence/`, `run/`) each with a `README.md`, and exactly the 31 controlled Markdown templates listed in `PACKAGE-MANIFEST.yaml#build_package_09_allowed_writes.template_files`.
- Build Package 09 templates must use only Markdown for template content; the only YAML file allowed under `templates/` is the single `templates/template-registry.yaml`. Every template must include the 14 required metadata fields (`template_id`, `template_name`, `owning_build_package: Build Package 09`, `status`, `purpose`, `intended_output`, `consumers`, `inputs_expected`, `placeholders`, `required_sections`, `prohibited_content`, `output_boundary`, `source_reference`, `version_notes`).
- Build Package 09 templates must use only the seven approved run-profile placeholder variables (`{{run_id}}`, `{{input_root}}`, `{{expected_root}}`, `{{output_root}}`, `{{mode}}`, `{{postback_execution_status}}`, `{{output_destination_mode}}`). Other run-profile schema fields (`sample_id`, `source_ticket_url`, `destination_ticket_url`, `destination_confluence_url`, `jira_connector_status`, `confluence_connector_status`, `rovo_mcp_available`, `mcp_execution_status`, `operator_name`, `reviewer_name`) must be referenced descriptively as `[copy from runs/{{run_id}}/run-profile.yaml#field_name]` and never as `{{field_name}}` placeholders. Build Package 09 templates must not introduce non-schema variables (`{{catalog_root}}`, `{{prompt_root}}`, `{{skill_root}}`, `{{template_root}}`, `{{diagram_root}}`, `{{validation_root}}`).
- Build Package 09 templates must reference catalog values via `<value-from-catalogs/...>` placeholder syntax. No template enumerates catalog entries inside its body and no template introduces a new controlled value (founder decision Q3). Catalog file paths cited in any template must resolve to an existing entry in `catalogs/catalog-registry.yaml#catalogs[]`.
- Build Package 09 templates must not contain severity computation, accuracy / qualitative score computation, AI Action Level computation, blueprint match decision, finding fact invention, recommendation prose invention, owner routing prose, or implementation-proof claims. Selection of severity, AI Action Level, blueprint match, and finding category remains with the Build Package 05 skills and human review; only `skills/rs/SK-ACCURACY-SCORE.md` produces an accuracy score and only when the run profile permits it.
- Build Package 09 templates must declare write paths only under `{{output_root}}` (or `{{output_root}}/dfd/` for DFD output templates, or append-only into `{{output_root}}/00-run-log.md` for run-family row templates). No template may declare a write to `config/`, `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, `blueprints/`, `templates/`, `samples/`, `diagrams/`, `docs/`, `release/`, `tools/`, `validation/`, or the old reference workspace.
- Build Package 09 Jira and Confluence draft templates must include the three-line post-back / publication claim block (`deferred` / `formatted_only` / `executed_by_operator`), a no-execution disclaimer, and a safety-check table. No template, prompt, skill, PRA, adapter, run output, or governance file may claim Jira post-back or Confluence publication unless `runs/{{run_id}}/run-profile.yaml#postback_execution_status` is `executed_by_operator`, `output_destination_mode` is `external_postback_executed`, and a `templates/run/postback-log-entry-row-template.md` row carrying timestamp + destination URL is appended to `{{output_root}}/00-run-log.md`.
- Build Package 09 must not modify any file under `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, or `blueprints/`, and must not modify `prompts/prompt-registry.yaml`, `skills/skill-registry.yaml`, `prototype-agents/prototype-agent-registry.yaml`, `catalogs/catalog-registry.yaml`, or `blueprints/blueprint-registry.yaml`. The Build Package 04 prompt registry, the Build Package 05 skill registry, the Build Package 06 prototype-agent registry, the Build Package 07 catalog registry, and the Build Package 08 blueprint registry are sealed for the duration of Build Package 09; template→prompt, template→skill, template→PRA, template→adapter, template→blueprint, and template→catalog consumer maps are recorded only in `templates/template-registry.yaml` and in `validation/template-registry-checklist.md` / `validation/template-consumption-checklist.md`. The Build Package 07 catalog count remains locked at 24 controlled-vocabulary YAML catalogs and the Build Package 08 blueprint count remains locked at 9 controlled YAML blueprints.
- Build Package 09 template count is fixed at 31 Markdown templates (27 output + 1 Jira + 1 Confluence + 2 run) plus 1 template registry plus 5 READMEs = 37 total files under `templates/` (founder decision Q5). Founder decision Q5 disallows a 32nd template, additional families, and template inventory expansion in this package.
- Build Package 09 must not claim runtime, cloud, Google ADK, Vertex/GCP, MCP, Jira post-back, Confluence publication, Rovo execution, database, Terraform, release packaging, diagram generation, or accuracy scoring inside any template. Templates do not execute the review.
- Build Package 09 must not create samples, expected baselines, run outputs, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, runtime services, cloud assets, deployed agents, MCP/Jira/Confluence/Rovo execution proof, ADK or Vertex/GCP code, database jobs, Terraform, release-export tools, or release binaries.
- Build Package 09 may extend `tools/Test-AisrafPackage.ps1` only for the five authorized compatibility changes (founder decision Q6): (a) update header/synopsis text from "Build Package 01-08" to "Build Package 01-09"; (b) remove `templates/` from the README-only folder rule; (c) add Check `08g-templates-content-limits` enforcing the exact Package 09 template folder layout; (d) extend Check 11's allowed validation-file list with `package-09-templates-checklist.md`, `template-registry-checklist.md`, and `template-consumption-checklist.md`; (e) preserve every other existing validator behaviour. Build Package 09 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, `validation/prompt-registry-checklist.md`, `validation/package-05-skills-checklist.md`, `validation/skill-registry-checklist.md`, `validation/package-06-agents-checklist.md`, `validation/agent-registry-checklist.md`, `validation/prompt-skill-agent-mapping-checklist.md`, `validation/package-07-catalogs-checklist.md`, `validation/catalog-registry-checklist.md`, `validation/catalog-consumption-checklist.md`, `validation/package-08-blueprints-checklist.md`, `validation/blueprint-registry-checklist.md`, or `validation/blueprint-catalog-consumption-checklist.md`, which are frozen acceptance evidence.
- No samples or expected baselines before Build Package 10. Build Package 10 authorizes the `samples/README.md` overwrite, `samples/sample-registry.yaml`, `samples/sample-001-dfd-crop/README.md`, exactly the 6 input files (`samples/sample-001-dfd-crop/inputs/dfd-crop.png`, `dfd-crop.mmd`, `dfd-legend-excerpt.md`, `cloud-triage-notes.md`, `review-transcript.md`, `intake-ticket.md`), and exactly the 26 Markdown expected baselines (17 RS at `samples/sample-001-dfd-crop/expected/expected-01..17-*.md` plus 9 DFD at `samples/sample-001-dfd-crop/expected/expected-dfd-01..09-*.md`).
- Build Package 10 expected baselines must be Markdown-only (founder decision Q1). JSON expected baselines are prohibited. Optional YAML front matter inside expected baselines is allowed for traceability and scoring metadata only and may not introduce execution claims.
- Build Package 10 must not create `samples/sample-001-dfd-crop/expected/expected-00-run-log.md` or any run-log expected baseline (founder decision Q2). Run logs are run artefacts and remain deferred to Build Package 11.
- Build Package 10 must include all 9 DFD subskill expected baselines for `sample-001-dfd-crop` (founder decision Q3): `expected-dfd-01-intake-quality-check.md` through `expected-dfd-09-extraction-summary.md`, mirroring `templates/output/output-dfd-01..09-*-template.md`.
- Build Package 10 must regenerate `samples/sample-001-dfd-crop/inputs/dfd-crop.png` from the v0.1.2 Mermaid source `samples/sample-001-dfd-crop/inputs/dfd-crop.mmd` via `mmdc` (founder decision Q4). The v0.01 sample PNG is read-only conceptual reference only and must not be bit-copied as v0.1.2 implementation truth.
- Build Package 10 must not pin a numeric accuracy verdict as v0.1.2 truth (founder decision Q5). The qualitative verdict in `samples/sample-001-dfd-crop/expected/expected-17-accuracy-score.md` is `PASS_READY_FOR_REVIEW`. The v0.01 reference 151/160 is recorded as `legacy_reference_score` only; numeric scoring is deferred to Build Package 11 run execution. No baseline may lower `expected_score_threshold` to force a pass.
- Build Package 10 expected baselines must mirror the named Package 09 template's `required_sections` exactly. Each baseline must declare its `mirrors_template`, `prompt`, `skill`, `owning_pra`, `adapter`, and `target_run_output` in YAML front matter. Allowed body placeholders are limited to the seven approved run-profile variables; other run-profile fields are referenced descriptively as `[copy from runs/{{run_id}}/run-profile.yaml#field_name]`. No baseline may introduce non-schema variables (`{{catalog_root}}`, `{{prompt_root}}`, `{{skill_root}}`, `{{template_root}}`, `{{diagram_root}}`, `{{validation_root}}`).
- Build Package 10 must not introduce new BP-* identifiers, new `material_missing_fact` IDs, new catalog identifiers, or new controlled values inside any sample artefact. Catalog values are referenced via `<value-from-catalogs/...>` placeholders; blueprint identifiers via `<value-from-blueprints/blueprint-registry.yaml#blueprint_id>`. The four-state match model `matched / candidate / no_match / unknown` applies. The sample-specific `C4P` token resolves under `catalogs/data-protection/data-class-catalog.yaml#DC-C5P` raw_pattern with a CL-MEDIUM confidence note recorded in `expected-03-legend-normalization.md`; no new catalog entry is introduced.
- Build Package 10 sample artefacts must be synthetic. No real PII, PAN, SSN, PHI, customer identifiers, internal employee identifiers, secrets, credentials, production endpoints, or vendor commercial product names asserted as mandatory may appear in any input or expected baseline. C5 / C5* / PCI / PAN / SSN / PHI tokens are explicitly out of scope for `sample-001-dfd-crop`.
- Build Package 10 must not claim Jira post-back, Confluence publication, Rovo / MCP / runtime / cloud / ADK / Vertex / GCP / database / Terraform execution. The Jira/Confluence formatter at CMP-08 / F8 in `sample-001-dfd-crop` is `formatted_only`. No baseline may assert `executed_by_operator`. The global rule that Jira post-back / Confluence publication requires `postback_execution_status: executed_by_operator` plus a matching post-back-row appended to `{{output_root}}/00-run-log.md` continues to apply.
- Build Package 10 expected baselines must not compute severity, finding category, AI Action Level, blueprint match, or accuracy score inside the body. Selection lives in the named skill plus human review; the baseline records the outcome only. Only `skills/rs/SK-ACCURACY-SCORE.md` produces an accuracy score and only when the run profile permits it.
- Build Package 10 must not modify any file under `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, `blueprints/`, `templates/`, or `config/`, and must not modify `prompts/prompt-registry.yaml`, `skills/skill-registry.yaml`, `prototype-agents/prototype-agent-registry.yaml`, `catalogs/catalog-registry.yaml`, `blueprints/blueprint-registry.yaml`, or `templates/template-registry.yaml`. The Build Package 04, 05, 06, 07, 08, and 09 registries are sealed for the duration of Build Package 10; sample→prompt, sample→skill, sample→PRA, sample→adapter, sample→catalog, sample→blueprint, and sample→template consumer maps are recorded only in `samples/sample-registry.yaml` and the validation checklists.
- Build Package 10 sample inventory is fixed at 1 active sample (`sample-001-dfd-crop`, gold-standard scored sample) plus 7 planned/deferred entries (`sample-002-annotation-token-matrix` through `sample-008-jira-confluence-formatted-output`) recorded in `samples/sample-registry.yaml#planned_or_deferred_samples` (founder decisions Q7 and Q8). No folder or file is created for sample-002 through sample-008. Sample-family expansion requires a separate governed package.
- Build Package 10 must not create live runs, prompts, skills, PRAs, `.agent.md` adapters, catalogs, blueprints, templates, diagrams, docs/runbooks, release artifacts, schemas outside `config/`, `.github/prompts/` wrappers, runtime services, cloud assets, deployed agents, MCP/Jira/Confluence/Rovo execution proof, ADK or Vertex/GCP code, database jobs, Terraform, release-export tools, or release binaries.
- Build Package 10 may extend `tools/Test-AisrafPackage.ps1` only for the four authorized compatibility changes (founder decision Q6): (a) update header/synopsis text from "Build Package 01-09" to "Build Package 01-10"; (b) remove `samples/` from the Check 09 README-only / deferred-sample rule; (c) add Check `08h-samples-content-limits` enforcing the exact Package 10 sample folder layout (`samples/README.md` + `samples/sample-registry.yaml` + `samples/sample-001-dfd-crop/` with `README.md`, `inputs/` containing exactly 6 files, and `expected/` containing exactly 26 Markdown files); (d) extend Check 11's allowed validation-file list with `package-10-samples-checklist.md`, `sample-registry-checklist.md`, and `sample-baseline-checklist.md`. Build Package 10 must not modify `validation/package-03-tools-checklist.md`, `validation/package-04-prompts-checklist.md`, `validation/prompt-registry-checklist.md`, `validation/package-05-skills-checklist.md`, `validation/skill-registry-checklist.md`, `validation/package-06-agents-checklist.md`, `validation/agent-registry-checklist.md`, `validation/prompt-skill-agent-mapping-checklist.md`, `validation/package-07-catalogs-checklist.md`, `validation/catalog-registry-checklist.md`, `validation/catalog-consumption-checklist.md`, `validation/package-08-blueprints-checklist.md`, `validation/blueprint-registry-checklist.md`, `validation/blueprint-catalog-consumption-checklist.md`, `validation/package-09-templates-checklist.md`, `validation/template-registry-checklist.md`, or `validation/template-consumption-checklist.md`, which are frozen acceptance evidence.
- No test claims without evidence.
- No Jira, Confluence, or MCP claims without execution evidence.
- No cloud, GCP, ADK, database, Terraform, runtime, or deployed-agent claims.
- No modification to the old reference workspace.
- No copying old stale release artifacts, failed diagrams, run proof, temporary reports, or generated outputs into this clean rebuild.
- No Build Package may invent a run-profile field outside `config/run-profile.schema.yaml`. Adding, removing, or renaming a field requires a Build Package 02 schema change in the same change set, with matching updates to `config/run-profile.template.yaml`, the samples, `config/run-profile.field-catalog.md`, and `config/run-profile.validation-rules.md`.
- No artifact, prompt, skill, PRA, runbook, scoring report, manifest entry, evidence record, diagram, or message may claim Jira post-back, Confluence publication, MCP execution, or connector use unless `postback_execution_status: executed_by_operator` (or the equivalent connector or MCP `executed_by_operator` value) is set AND `output_root/00-run-log.md` records timestamp, destination URL, and operator role for that action.
- Build Package 03 tools must resolve every run-profile field through `config/run-profile.template.yaml` and `config/run-profile.schema.yaml`. A tool may not invent, rename, or silently default any field outside that schema; adding a field requires a Build Package 02 schema change in the same change set.
- Build Package 03 tools must not write outside `runs/<run_id>/`. They must not modify `config/`, `prompts/`, `skills/`, `prototype-agents/`, `.agents/`, `catalogs/`, `blueprints/`, `templates/`, `samples/`, `diagrams/`, `docs/`, `release/`, the old reference workspace, or any path with a drive letter, leading `/`, or `..` segment.
- Build Package 03 tools must not claim execution of prompts, skills, PRAs, `.agent.md` adapters, Jira, Confluence, Rovo, MCP, scoring, diagram generation, runbook generation, or release export. Console output and run-log entries must describe only what the tool did.
- Build Package 03 must not introduce `tools/Export-AisrafRelease.ps1` or any other release-export tool. Release packaging is owned by Build Package 15 and gated by founder approval.
- Smoke-run folders created by Build Package 03 tooling must be removed before human git stage. No `runs/<run_id>/` folder may be committed before Build Package 11.
