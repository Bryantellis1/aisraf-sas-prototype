# config/

Root Area: Root Area 04.

Owning Build Package: Build Package 02 — Config and run-profile variable model.

## Purpose

Reserved configuration and run-profile variable model folder.

## What Belongs Here

Build Package 02 may add run-profile variables, safe defaults, config registries, and examples for `run_id`, `sample_id`, `input_root`, `output_root`, and `expected_root`.

## What Does Not Belong Here

Secrets, credentials, local tokens, scripts, runtime services, schemas in Build Package 01, generated reports, run outputs, release artifacts, or cloud configuration claims.

## When Populated

Build Package 02.

## Current Build Package 01 Status

Reserved with README only.
