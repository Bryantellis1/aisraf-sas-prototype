# SVG Notes

No SVG renders are included in the WP-13 first public visual pack. The accepted public-quality assets are PNG files under `../png/`, with generated PNG thumbnails under `../thumbnails/`.

Future SVG files require exact validator allow-list entries before they are committed.
