# prompts/

Root Area: Root Area 06.

Owning Build Package: Build Package 04 — Prompts and prompt registry.

## Purpose

Reserved canonical prompt-card folder.

## What Belongs Here

Build Package 04 may add reusable instruction files that tell Copilot or an agent how to perform a task using resolved variables such as `input_root`, `output_root`, `expected_root`, `run_id`, and `sample_id`.

## What Does Not Belong Here

Prompt cards in Build Package 01, skill contracts, PRA specs, adapters, catalogs, generated outputs, scripts, schemas, diagrams, or runtime code.

## When Populated

Build Package 04.

## Current Build Package 01 Status

Reserved with README only. No prompt cards exist yet.
